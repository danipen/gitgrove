name: Install
description: Set up a pinned Bun, restore its download cache, and install deps with a retry for transient registry blips.

inputs:
  bun-version:
    description: Bun version to install (kept identical to the local toolchain).
    required: true

runs:
  using: composite
  steps:
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: ${{ inputs.bun-version }}

    # Cache only Bun's global download cache — NOT node_modules, which holds
    # platform-specific binaries (electron, the Biome native binary). The key is
    # scoped by os AND arch so the Windows x64 and arm64 legs never share (and
    # corrupt) each other's cache.
    - uses: actions/cache@v4
      with:
        path: ~/.bun/install/cache
        key: bun-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('bun.lock') }}
        restore-keys: |
          bun-${{ runner.os }}-${{ runner.arch }}-

    # Cache the downloaded Electron binary archive (electron's postinstall runs
    # on every `bun install`, fetching a ~100 MB platform zip from GitHub
    # releases — a flaky download is the single most common CI failure). Keyed
    # by os+arch+lockfile so a pinned-version bump invalidates it. These are
    # @electron/get's default cache dirs; only the current OS's path exists, the
    # others are silently skipped. A warm cache makes the postinstall a local
    # unzip with no network, removing the flake at its source.
    #
    # The `v2` salt lets us abandon a *corrupt* cached archive without waiting
    # for a bun.lock change: a download interrupted mid-save leaves a truncated
    # zip that @electron/get still treats as a cache hit, so electron's
    # postinstall silently writes a broken (or no) binary on every run that
    # restores it. Bump the salt to force every runner to re-download cleanly.
    - uses: actions/cache@v4
      with:
        path: |
          ~/.cache/electron
          ~/Library/Caches/electron
          ~/AppData/Local/electron/Cache
        key: electron-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('bun.lock') }}

    # GitHub's package registry occasionally drops connections mid-install
    # (ConnectionRefused / FailedToOpenSocket). A warm cache helps, but a cold
    # cache still hits the network, so retry a couple of times before failing.
    - name: Install dependencies
      shell: bash
      run: |
        for attempt in 1 2 3; do
          if bun install --frozen-lockfile; then
            exit 0
          fi
          echo "::warning::bun install failed (attempt ${attempt}/3); retrying in $((attempt * 10))s"
          sleep $((attempt * 10))
        done
        echo "::error::bun install failed after 3 attempts"
        exit 1
